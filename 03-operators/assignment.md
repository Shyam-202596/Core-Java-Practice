# 03 - Operators Assignment

## Objective

This assignment focuses on Java operators, expression evaluation, short-circuit behavior, type promotion, precedence, and practical coding problems.

Keep answers short and code clean.

---

# Theory and Interview Questions

## Q1. What is an operator in Java?

Give one example.

---

## Q2. List the main types of operators in Java.

---

## Q3. What is the difference between `/` and `%` operators?

Give examples.

---

## Q4. What is integer division in Java?

Predict the output:

```java
System.out.println(10 / 3);
```

---

*# Q5. What is the difference betwe*n `=` and `==`?

---

## Q6. What *s the difference between relationa* and logical operators?

Give exam*les.

---

## Q7. What is short-ci*cuit evaluation?

Explain with `&&* and `||`.

---

## Q8. Predict th* output.

```java
int a = 5;
int b*= ++a;

System.out.println(a);
Sys*em.out.println(b);
```

---

## Q9* Predict the output.

```java
int * = 5;
int b = a++;

System.out.pri*tln(a);
System.out.println(b);
```*
---

## Q10. Predict the output.
*```java
int result = 10 + 5 * 2;
S*stem.out.println(result);
```

---*
## Q11. What is operator preceden*e?

Why should we use parentheses *n complex expressions?

---

## Q1*. What is the ternary operator?

W*ite one example.

---

## Q13. Wha* is the difference between `&&` an* `&`?

---

## Q14. What is the di*ference between `||` and `|`?

---*
## Q15. What is the use of `insta*ceof` operator?

---

# Coding Que*tions

## Q16. Arithmetic Operatio*s

Write a Java program to take tw* numbers as input and print:

1. A*dition
2. Subtraction
3. Multiplic*tion
4. Division
5. Remainder

---*
## Q17. Even or Odd Using Modulus*
Write a Java program to check whe*her a number is even or odd using *%`.

---

## Q18. Last Digit of a *umber

Write a Java program to pri*t the last digit of a given number*

Example:

```text
Input: 12345
O*tput: 5
```

---

## Q19. Simple C*lculator

Write a Java program to *ake two numbers and an operator as*input.

Supported operators:

```t*xt
+
-
*
/
%
```

Use `if-else`.

*--

## Q20. Largest of Two Numbers*Using Ternary Operator

Write a Ja*a program to find the largest of t*o numbers using ternary operator.
*---

## Q21. Positive, Negative, o* Zero

Write a Java program to che*k whether a number is positive, ne*ative, or zero.

Use relational op*rators.

---

## Q22. Eligibility *heck

Write a Java program to chec* whether a person is eligible to v*te.

Conditions:

```text
age >= 1*
hasVoterId == true
```

Use logic*l operators.

---

## Q23. Range C*eck

Write a Java program to check*whether a number is between `1` an* `100`.

Use logical AND.

---

##*Q24. Leap Year Check

Write a Java*program to check whether a year is*leap year or not.

Use logical ope*ators.

Logic:

```text
year divis*ble by 400
OR
year divisible by 4 *ND not divisible by 100
```

---

*# Q25. Swap Two Numbers

Write a J*va program to swap two numbers wit*out using a third variable.

Use a*ithmetic operators.

---

## Q26. *re-Increment and Post-Increment De*o

Write a Java program to demonst*ate the difference between pre-inc*ement and post-increment.

---

##*Q27. Bitwise AND, OR, XOR

Write a*Java program to perform bitwise AN*, OR, and XOR on two numbers.

Exa*ple:

```java
int a = 5;
int b = 3*
```

---

## Q28. Left Shift and *ight Shift

Write a Java program t* demonstrate left shift and right shift operators.

Example:

```java
int num = 8;
```

---

## Q29. Ternary Grade Program

Write a Java program to print:

```text
Pass
```

if marks are greater than or equal to `35`, otherwise print:

```text
Fail
```

Use ternary operator.

---

## Q30. Expression Evaluation

Predict the output and then verify using Java.

```java
int a = 10;
int b = 5;
int c = 2;

int result = a + b * c - a / c;

System.out.println(result);
```

---

# Submission Checklist

Before submitting, verify:

- Programs compile successfully.
- Division by zero is handled where required.
- Integer division behavior is understood.
- Pre-increment and post-increment are clear.
- Logical and bitwise operators are not confused.
- Parentheses are used for complex expressions.
- Code is simple and readable.